[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: fast isolated unit tests
    integration: API/database integration tests
    e2e: end-to-end scenario tests across multiple modules
